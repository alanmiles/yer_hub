<%= link_to image_tag("pageback.png", :title => "Nationality list"), nationalities_path, :class => "magnify" %>

<%= form_for(@nationality) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit :class => "create round" %>
  </div>
<% end %>
